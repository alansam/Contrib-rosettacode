#!/usr/bin/env nr
/* NetRexx */
options replace format comments java nocrossref symbols nobinary

receiver = ''
commando = 'ls -oa'

address 'sh' commando with output stem receiver

say 'lines received:' receiver[0]

loop label ln ln = 1 to receiver[0]
  say ln.right(3)':' receiver[ln]
end ln

